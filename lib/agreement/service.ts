import { Prisma, type AgreementStatus } from "@prisma/client"; import { prisma } from "@/lib/db"; import { createSigningToken, randomPublicId, sha256 } from "@/lib/crypto"; import { appBaseUrl } from "@/lib/env"; import { formatAest } from "@/lib/format"; import { AGREEMENT_VERSION, createAgreementSnapshot } from "@/lib/agreement/content"; import { sendSigningRequest } from "@/lib/email"; import { logInternalFailure } from "@/lib/internal-log"; import { coverageLabel, formatMoney } from "@/lib/services";
function date(value:Date){return value.toISOString().slice(0,10);}
export async function createAgreementDraft(purchaseId:string,agentName="Legacy system"){const p=await prisma.purchase.findUnique({where:{id:purchaseId},include:{customer:true}});if(!p)throw new Error("Purchase not found");if(!p.serviceType||!p.coverageType||!p.coverageStartDate||p.legacyReviewRequired)throw new Error("Legacy sale requires administrator review before an agreement can be created");for(let attempt=0;attempt<8;attempt++){const agreementNumber=randomPublicId("CSA-AGR",8);const snapshot=createAgreementSnapshot({agreementNumber,customerId:p.customer.customerId,customerName:p.customer.name,email:p.customer.email,address:p.customer.address,serviceType:p.serviceType,amount:formatMoney(p.salesAmount,p.currency),saleDate:date(p.saleDate),coverageStart:date(p.coverageStartDate),coverageEnd:p.coverageEndDate?date(p.coverageEndDate):undefined,coverageType:p.coverageType,tenureValue:p.tenureValue,tenureUnit:p.tenureUnit});try{return await prisma.agreement.create({data:{customerId:p.customerId,purchaseId:p.id,agreementNumber,agreementVersion:p.agreementTemplateVersion||AGREEMENT_VERSION,agreementContentSnapshot:snapshot,lastActionAgentName:agentName,events:{create:{eventType:"DRAFT_CREATED",metadata:{serviceType:p.serviceType,coverage:coverageLabel(p)}}}}});}catch(error){if(!(error instanceof Prisma.PrismaClientKnownRequestError)||error.code!=="P2002"||attempt===7)throw error;}}throw new Error("Unable to allocate an agreement number");}
const sendable:AgreementStatus[]=["DRAFT","SENT","VIEWED","EXPIRED"];
export async function issueSigningRequest(agreementId:string,eventType:"SENT"|"RESENT",agentName="Legacy system"){const a=await prisma.agreement.findUnique({where:{id:agreementId},include:{customer:true,purchase:true}});if(!a||!sendable.includes(a.status))throw new Error("Agreement cannot be sent in its current state");const previous={tokenHash:a.tokenHash,tokenExpiresAt:a.tokenExpiresAt,status:a.status,sentAt:a.sentAt};const{token,hash}=createSigningToken(),expiresAt=new Date(Date.now()+7*86400000);await prisma.agreement.update({where:{id:a.id},data:{tokenHash:hash,tokenExpiresAt:expiresAt,tokenUsedAt:null,status:"SENT",sentAt:new Date(),lastActionAgentName:agentName,events:{create:{eventType,metadata:{expiresAt:expiresAt.toISOString()}}}}});let resendEmailId:string;try{resendEmailId=await sendSigningRequest(a.customer.email,{customerName:a.customer.name,customerId:a.customer.customerId,signingUrl:`${appBaseUrl()}/sign/${token}`,expiresAt:formatAest(expiresAt)});}catch(error){try{await prisma.agreement.update({where:{id:a.id},data:{...previous,events:{create:{eventType:"SIGNING_EMAIL_FAILED"}}}});}catch(rollback){logInternalFailure("agreement_invitation_failure_rollback",rollback);}throw error;}try{await prisma.agreement.update({where:{id:a.id},data:{resendCustomerEmailId:resendEmailId,events:{create:{eventType:"SIGNING_EMAIL_DELIVERED",metadata:{resendEmailId}}}}});}catch(error){logInternalFailure("agreement_invitation_success_database_update",error);}return{resendEmailId,persistenceFailed:false};}
export async function findAgreementByRawToken(token:string){const agreement=await prisma.agreement.findUnique({where:{tokenHash:sha256(token)},include:{customer:true,purchase:true,events:{orderBy:{timestamp:"asc"}}}});if(!agreement)return{kind:"invalid" as const};if(agreement.status==="VOID")return{kind:"void" as const};if(agreement.status==="SIGNED"||agreement.tokenUsedAt)return{kind:"used" as const,agreement};if(!agreement.tokenExpiresAt||agreement.tokenExpiresAt<=new Date()){if(agreement.status!=="EXPIRED")await prisma.agreement.update({where:{id:agreement.id},data:{status:"EXPIRED",events:{create:{eventType:"TOKEN_EXPIRED"}}}});return{kind:"expired" as const,agreement};}if(!(["SENT","VIEWED"] as AgreementStatus[]).includes(agreement.status))return{kind:"invalid" as const};return{kind:"valid" as const,agreement};}
export async function recordAgreementViewed(agreementId:string,ip:string,userAgent:string){return prisma.$transaction(async tx=>{const u=await tx.agreement.updateMany({where:{id:agreementId,viewedAt:null,status:"SENT"},data:{viewedAt:new Date(),status:"VIEWED"}});if(u.count===1)await tx.agreementEvent.create({data:{agreementId,eventType:"VIEWED",ipAddress:ip,userAgent}});});}
