import { randomUUID } from "node:crypto"; import { prisma } from "@/lib/db"; import { requireAgent } from "@/lib/auth"; import { maskEmail, maskName } from "@/lib/masking"; import { verifyPortalToken } from "@/lib/portal-token"; import { SERVICES, coverageLabel, formatMoney } from "@/lib/services"; import { InternalShell } from "@/components/internal/InternalShell"; import { agentLogoutAction } from "./login/actions"; import { addAgentNoteAction, createConfirmationAction, exactLookupAction, resendConfirmationAction, sendAgreementAction } from "./actions";
type Params={success?:string;error?:string;record?:string;match?:string;matchName?:string;matchEmail?:string};
export const metadata={title:"Agent Portal",robots:{index:false,follow:false}};
export default async function PortalPage({searchParams}:{searchParams:Promise<Params>}){const session=await requireAgent(),params=await searchParams,payload=params.record?verifyPortalToken(params.record,session.id,"record"):null,customer=payload?await prisma.customer.findUnique({where:{id:payload.customerId},include:{purchases:{orderBy:{createdAt:"desc"},take:1},agreements:{orderBy:{createdAt:"desc"},take:1}}}):null,today=new Date().toISOString().slice(0,10),latest=customer?.purchases[0];return <InternalShell role="Agent" logoutAction={agentLogoutAction}><p className="internal-eyebrow">Customer operations</p><h1>Service confirmation and agreement</h1><p className="internal-lead">Select the exact service sold, record its custom amount and coverage, then issue the service-specific customer documents. Customer browsing remains unavailable.</p>{params.success&&<div className="internal-alert success">{params.success}</div>}{params.error&&<div className="internal-alert error">{params.error}</div>}{params.match&&<div className="internal-alert error"><strong>Possible existing customer</strong><span>{params.matchName} · {params.matchEmail}. Verify identity and resubmit to reuse the permanent Customer ID.</span></div>}<div className="internal-grid"><section className="internal-card" style={{padding:"1.5rem"}}><h2>New service sale</h2><form action={createConfirmationAction} className="internal-form"><input type="hidden" name="csrfToken" value={session.csrfToken}/><input type="hidden" name="submissionKey" value={randomUUID()}/><input type="hidden" name="idempotencyKey" value={randomUUID()}/>{params.match&&<input type="hidden" name="reuseToken" value={params.match}/>}<fieldset><legend>SERVICE SELECTED</legend><div className="service-choice"><label><input type="radio" name="serviceType" value="NETWORK_SECURITY" required/><strong>Network Security</strong><span>{SERVICES.NETWORK_SECURITY.summary}</span></label><label><input type="radio" name="serviceType" value="IDENTITY_IP_PROTECTOR" required/><strong>Identity &amp; IP Protector</strong><span>{SERVICES.IDENTITY_IP_PROTECTOR.summary}</span></label></div></fieldset><h3>Customer details</h3><div className="internal-form-grid"><label>First name<input name="firstName" required/></label><label>Last name<input name="lastName"/></label></div><label>Email<input name="email" type="email" required/></label><div className="internal-form-grid"><label>Phone<input name="phone" required/></label><label>Alternate phone<input name="alternatePhone"/></label></div><label>Covered address<textarea name="address" required rows={3}/></label><h3>Sale and coverage</h3><div className="internal-form-grid"><label>Custom sales amount<input name="salesAmount" inputMode="decimal" required/></label><label>Currency<select name="currency" defaultValue="AUD"><option>AUD</option><option>USD</option><option>GBP</option></select></label></div><div className="internal-form-grid"><label>Sale date<input name="saleDate" type="date" defaultValue={today} required/></label><label>Coverage start date<input name="coverageStartDate" type="date" defaultValue={today} required/></label></div><div className="internal-form-grid"><label>Coverage type<select name="coverageType" defaultValue="FIXED"><option value="FIXED">Fixed duration</option><option value="LIFETIME">Lifetime Coverage</option></select></label><label>Tenure value (leave blank for Lifetime)<input name="tenureValue" type="number" min="1" step="1"/></label></div><label>Tenure unit (ignored for Lifetime)<select name="tenureUnit" defaultValue="YEARS"><option value="DAYS">Days</option><option value="MONTHS">Months</option><option value="YEARS">Years</option></select></label><label>Agent name<input name="agentName" required/></label><h3>Internal service details</h3><p className="internal-only">Internal only — never included in customer emails or PDFs.</p><label>Primary issue<textarea name="primaryIssue" required rows={3}/></label><label>Service details<textarea name="serviceDetails" required rows={4}/></label><label>Internal notes<textarea name="internalNotes" rows={3}/></label><button className="internal-primary">{params.match?"Reuse customer and send":"Save and send confirmation"}</button></form></section><div><section className="internal-card" style={{padding:"1.5rem"}}><h2>Exact customer lookup</h2><form action={exactLookupAction} className="internal-form"><input type="hidden" name="csrfToken" value={session.csrfToken}/><label>Exact Customer ID or email<input name="query" required placeholder="CS-2026-000123 or customer@example.com"/></label><button className="internal-secondary">Retrieve customer</button></form></section>{customer&&params.record&&<section className="internal-card" style={{padding:"1.5rem",marginTop:"1rem"}}><p className="internal-eyebrow">Retrieved record</p><h2>{maskName(customer.firstName,customer.lastName)}</h2><div className="summary-grid"><span>Customer ID<strong>{customer.customerId}</strong></span><span>Email<strong>{maskEmail(customer.email)}</strong></span><span>Service<strong>{latest?.serviceType?SERVICES[latest.serviceType].name:"Legacy — review required"}</strong></span><span>Amount<strong>{latest?formatMoney(latest.salesAmount,latest.currency):"None"}</strong></span><span>Coverage<strong>{latest?coverageLabel(latest):"None"}</strong></span><span>Agreement<strong>{customer.agreements[0]?.status||"None"}</strong></span></div><form action={resendConfirmationAction} className="internal-form" style={{marginTop:"1rem"}}><input type="hidden" name="recordToken" value={params.record}/><input type="hidden" name="csrfToken" value={session.csrfToken}/><input type="hidden" name="idempotencyKey" value={randomUUID()}/><label>Agent name<input name="agentName" required/></label><button className="internal-secondary">Resend correct confirmation</button></form><form action={sendAgreementAction} className="internal-form" style={{marginTop:"1rem"}}><input type="hidden" name="recordToken" value={params.record}/><input type="hidden" name="csrfToken" value={session.csrfToken}/><input type="hidden" name="idempotencyKey" value={randomUUID()}/><label>Agent name<input name="agentName" required/></label><button className="internal-primary">Send correct agreement</button></form><form action={addAgentNoteAction} className="internal-form" style={{marginTop:"1rem"}}><input type="hidden" name="recordToken" value={params.record}/><input type="hidden" name="csrfToken" value={session.csrfToken}/><label>Agent name<input name="agentName" required/></label><label>Append internal note<textarea name="noteText" required rows={3}/></label><button className="internal-secondary">Add note</button></form></section>}</div></div></InternalShell>;}
