import Link from "next/link"; import { SiteShell,PageHero,Breadcrumbs } from "@/components/public/SiteShell"; import { publicMetadata } from "@/lib/public-metadata"; import { SERVICES } from "@/lib/services";
export const metadata=publicMetadata("CyberShield Services","Explore Network Security and Identity & IP Protector support, customised for each customer and coverage period.","/services");
export default function Services(){return <SiteShell><PageHero eyebrow="Services" title="Cybersecurity support shaped around the customer" copy="CyberShield offers two service categories. Price, currency, coverage period and service scope are recorded for each individual sale."/><div className="site-wrap"><Breadcrumbs items={[["Home","/"],["Services","/services"]]}/><section className="public-section compact"><div className="service-grid">{(["NETWORK_SECURITY","IDENTITY_IP_PROTECTOR"] as const).map((key)=><article className="service-card" key={key}><h2>{SERVICES[key].name}</h2><p>{SERVICES[key].summary}</p><ul>{SERVICES[key].areas.map(x=><li key={x}>{x}</li>)}</ul><Link href={key==="NETWORK_SECURITY"?"/services/network-security":"/services/identity-ip-protector"}>View service details →</Link></article>)}</div></section></div></SiteShell>}
