import Link from "next/link"; import { SiteShell,PageHero,Breadcrumbs } from "@/components/public/SiteShell";
export function ServiceDetail({name,heading,intro,areas,examples,explain}:{name:string;heading:string;intro:string;areas:string[];examples:{title:string;body:string}[];explain?:{title:string;body:string}[]}){return <SiteShell><PageHero eyebrow={name} title={heading} copy={intro}/><div className="site-wrap"><Breadcrumbs items={[["Home","/"],["Services","/services"],[name,"#"]]}/><section className="public-section compact"><div className="content-grid"><div><p className="kicker">Depending on the purchased scope</p><h2>Service areas may include</h2><ul className="feature-list">{areas.map(x=><li key={x}>{x}</li>)}</ul></div><aside className="scope-note"><h2>Support, not absolute promises</h2><p>CyberShield provides assistance, guidance, review, configuration help and practical protective steps. Features depend on the recorded service scope and signed agreement.</p></aside></div></section>{explain&&<section className="public-section muted"><div className="info-grid">{explain.map(x=><article key={x.title}><h2>{x.title}</h2><p>{x.body}</p></article>)}</div></section>}<section className="public-section"><p className="kicker">Practical examples</p><h2>When customers may contact CyberShield</h2><div className="example-cards">{examples.map(x=><article key={x.title}><h3>{x.title}</h3><p>{x.body}</p></article>)}</div></section><section className="cta-panel"><h2>Need practical cybersecurity assistance?</h2><p>Contact CyberShield to discuss the concern and available service scope.</p><Link className="btn-primary" href="/contact">Contact CyberShield</Link></section></div></SiteShell>}

